spring:
  application:
    name: pickly
  profiles:
    group:
      test: test
      local: local, datasource, swagger
      dev: dev, datasource, swagger
      prod: prod, datasource
    include:
      - actuator
      - security
    active: ${SPRING_PROFILES_ACTIVE:local}
